import { Button } from "@/components/primitives/Button";
import { LineIcon } from "@/components/icons/LineIcon";
import type { HomePage } from "@/lib/types";

type Props={banner:HomePage["ctaBanner"];eyebrow?:string;secondaryCta?:{label:string;href:string};notes?:string[]};
export function CtaBanner({banner,eyebrow="Get started",secondaryCta,notes}:Props){return <section className="mesh-dark border-y border-white/10 text-white"><div className="container-x flex flex-col items-center gap-5 py-24 text-center max-[767px]:py-16"><span className="eyebrow text-gold">{eyebrow}</span><h2 className="m-0 max-w-[20ch] font-display text-[clamp(30px,3.4vw,44px)] font-medium leading-[1.14]">{banner.heading}</h2><p className="m-0 max-w-[52ch] text-[15.5px] leading-relaxed text-dark-body">{banner.lead}</p><div className="mt-2 flex flex-wrap justify-center gap-3"><Button href={banner.cta.href} variant="inverse" size="sm">{banner.cta.label}<LineIcon name="arrow-right" size={15}/></Button><Button href={secondaryCta?.href??"tel:+13148281564"} variant="inverse-outline" size="sm">{secondaryCta?.label??"Call us today"}</Button></div>{notes?.length?<p className="m-0 text-[12px] text-dark-label">{notes.join(" · ")}</p>:null}</div></section>}
