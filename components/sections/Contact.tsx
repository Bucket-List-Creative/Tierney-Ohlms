import { ContactForm } from "@/components/sections/ContactForm";
import { IconTile } from "@/components/primitives/IconTile";
import type { IconKey } from "@/components/icons/registry";
import type { HomePage,SiteSettings } from "@/lib/types";

export function Contact({contact,site}:{contact:HomePage["contact"];site:SiteSettings}){const rows:{icon:IconKey;label:string;value:React.ReactNode}[]=[{icon:"phone",label:"Call us",value:<a href={site.phoneHref}>{site.phone}</a>},{icon:"mail",label:"Email us",value:<a href={`mailto:${site.email}`}>{site.email}</a>},{icon:"location",label:"Visit us",value:<span>{site.addressLine1}<br/>{site.addressLine2}</span>}];return <section id="contact" className="border-t border-rule bg-white"><div className="container-x grid grid-cols-[.85fr_1.15fr] items-start gap-16 py-24 max-[980px]:grid-cols-1 max-[980px]:gap-10 max-[767px]:py-16"><div className="flex flex-col gap-7"><header className="flex flex-col gap-4"><span className="eyebrow">{contact.eyebrow}</span><h2 className="m-0 font-display text-[clamp(28px,3vw,40px)] font-medium leading-[1.14]">Send us a <em className="gradient-text">message.</em></h2><p className="m-0 text-[15.5px] leading-relaxed text-slate">{contact.lead}</p></header><ul className="m-0 p-0">{rows.map(row=><li key={row.label} className="group flex items-center gap-3.5 border-t border-rule py-4 transition-all hover:pl-3"><IconTile icon={row.icon} tile={40} size={18}/><span className="flex flex-col"><span className="font-mono text-[10px] uppercase tracking-[.14em] text-dark-label">{row.label}</span><span className="text-[14px] leading-snug text-ink">{row.value}</span></span></li>)}</ul></div><ContactForm serviceOptions={contact.serviceOptions} contactEmail={site.email}/></div></section>}
